version: "3.7"
services:
  think-vue-hello:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8080:80'
    # volumes:
    #   - .:/app
    #   - ./dist:/etc/nginx/html
    #   - ./config/nginx:/etc/nginx/conf.d
    restart: always

